window.docContents[114].resolve({"/Tactics/#apply":{"contents":"Summary: If we have the goal ⊢ Q, and a proof of h : P → Q, we only need to find a proof for P. This transition happens by apply h.\n\n* Proof state* Tactic* New proof state* h : P → Q  ⊢ Q* apply h* h : P → Q  ⊢ P* h : ¬P  ⊢ False* apply h* h : ¬P  ⊢ P* h₁ : P → Q  h₂ : Q → R   ⊢ R* apply h₂ h₁* h₁ : P → Q  h₂ : Q → R   ⊢ P\n\nThe apply-tactics works iteratively. This means that if apply h makes no progress, it uses the placeholder _ and tries to make apply h _.\n\nRemarks:\n\n* apply works up to equality by definition. This can be seen in the example above, where ¬P ↔ (P → False) is true by definition.* apply h is frequently identical to refine ?_.* If the use of apply closes the current goal, you might as well use exact instead of apply.\n\n example (hP : P) (hPQ : P → Q) (hPQR : P → Q → R) : R := by\n  apply hPQR\n  · exact hP\n  · apply hPQ\n    exact hP\nexample (n : ℕ) (hn : 0 < n) : n ≤ 2 * (n * n) := by\n  have h₁ : n ≤ n * n := by\n    exact Nat.le_mul_of_pos_left n hn\n  apply le_trans h₁\n  have h₂ (k : ℕ) : k ≤ 2 * k := by\n    exact Nat.le_mul_of_pos_left k Nat.zero_lt_two\n  apply h₂ (n * n)\napply e tries to match the current goal against the conclusion of e's type.\nIf it succeeds, then the tactic returns as many subgoals as the number of premises that\nhave not been fixed by type inference or type class resolution.\nNon-dependent premises are added before dependent ones.The apply tactic uses higher-order pattern matching, type class resolution,\nand first-order unification with dependent types.Extensions:* * apply (config := cfg) e allows for additional configuration (see Lean.Meta.ApplyConfig):* newGoals controls which new goals are added by apply, in which order.* -synthAssignedInstances will not synthesize instance implicit arguments if they have been\nassigned by isDefEq.* +allowSynthFailures will create new goals when instance synthesis fails, rather than erroring.* +approx enables isDefEq approximations (see Lean.Meta.approxDefEq).\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Tactics","header":"4.2. apply","id":"/Tactics/#apply"}});