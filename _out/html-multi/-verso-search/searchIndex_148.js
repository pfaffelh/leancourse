window.docContents[148].resolve({"/Advanced-Mathematics/#nhds-filter":{"contents":"For a point x in a topological space, nhds x is the filter of\nneighborhoods of x. A set s is a neighborhood of x if there is an\nopen set U with x ∈ U ⊆ s.\n\nA set is called a neighborhood of x if it contains an open set around x. The set of all\nneighborhoods of x forms a filter, the neighborhood filter at x, is here defined as the\ninfimum over the principal filters of all open sets containing x.\n\n\n\nThe neighborhood filter is the bridge between topology and the filter\nframework.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Advanced Mathematics\u0009Topology via Filters","header":"5.4.5. The neighborhood filter nhds","id":"/Advanced-Mathematics/#nhds-filter"},"/Advanced-Topics/#simp-tactic":{"contents":"The simp tactic is the workhorse of Lean automation. It applies a set of rewriting rules (called simp lemmas) repeatedly until no more rules apply. A lemma is marked as a simp lemma by adding the @[simp] attribute to it. Mathlib contains thousands of such lemmas.\n\nBasic usage:\n\nexample (n : ℕ) : n + 0 = n := by simp\nexample (s : Set ℝ) : s ∩ Set.univ = s := by simp\nexample (n : ℕ) : n ∈ ({n} : Set ℕ) := by simp\n\n\nThe variant simp only [...] restricts simp to use only the listed lemmas. This is preferred in Mathlib contributions because it makes proofs more robust against changes in the simp set.\n\nexample (n : ℕ) : 0 + n + 0 = n := by simp only [Nat.add_zero, Nat.zero_add]\n\n\nThe variant simp_all applies simp to all hypotheses and the goal simultaneously. This is more powerful than calling simp at * because simplifications in one hypothesis can unlock simplifications elsewhere.\n\nexample (n m : ℕ) (h : n + 0 = m) : m = n := by simp_all\n\n\nPerformance warning: The global simp call (without only) searches through all simp lemmas, which can be slow. In library code, prefer simp only. In interactive exploration, plain simp is fine. You can use simp? to find out which lemmas simp uses, and then replace simp by simp only [...].\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Advanced Topics\u0009Proof Automation","header":"6.1.1. The  simp  tactic","id":"/Advanced-Topics/#simp-tactic"},"/Lean/#calc":{"contents":"Summary: As the word suggests, calc is about concrete calculations. This is not a tactic, but a lean mode. This means that you can enter this mode (with the word calc) and enter calculation steps and proofs that each individual calculation step is correct.\n\nExamples\n\nHere is a proof of the first binomial formula that only comes about by rewriting of calculating properties from the mathlib.\n\nexample (n : ℕ): (n+1)^2 = n^2 + 2*n + 1 := by\n  have h : n + n = 2 * n := by\n    nth_rewrite 1 [← one_mul n]\n    nth_rewrite 2 [← one_mul n]\n    rw [← add_mul]\n    rfl\n  calc (n+1)^2 = (n+1) * (n+1) := by rw [pow_two]\n  _ = (n + 1) * n + (n + 1) * 1 := by rw [mul_add]\n  _ = n * n + 1 * n + (n + 1) := by\n    rw [add_mul, mul_one (n + 1)]\n  _ = n^2 + n + (n + 1) := by rw [one_mul, ← pow_two]\n  _ = n^2 + (n + n + 1) := by\n    rw [add_assoc, ← add_assoc n n 1]\n  _ = n^2 + 2*n + 1 := by rw [← add_assoc, ← h]\n\n\nThe same can be achieved without the calc mode, like this:\n\nexample (n : ℕ): (n+1)^2 = n^2 + 2*n + 1 := by\n  have h : n + n = 2*n, by { nth_rewrite 0 ← one_mul n,\n  nth_rewrite 1 ← one_mul n, rw ← add_mul, },\n  rw [pow_two, mul_add, add_mul, mul_one (n+1), one_mul,\n  ← pow_two, add_assoc, ← add_assoc n n 1,\n  ← add_assoc, ← h],\n\n\nHowever, this is much less readable.\n\nRemarks\n\n* The exact notation is important in calc mode.* The calc mode not only works for equalities, but also for inequalities, subset-relations etc.* The example above can be solved easily using linarith or ring.* In order to generate a proof in calc mode, one can do it as follows (see the example below):* give the exact calculation steps without proof (using by sorry)* fill in the proofs which are left over.\n\n Here is how to start the proof of the binomial formula. First, leave out all proofs:example (n : ℕ) : n + n = 2*n := by\n  calc n + n = 1 * n + 1 * n := by sorry\n  _ = (1 + 1) * n := by sorry\n  _ = 2 * n := by sorry\nThen, fill in the detailsexample (n : ℕ) : n + n = 2*n := by\n  calc n + n = 1 * n + 1 * n := by rw [one_mul]\n  _ = (1 + 1) * n := by rw [add_mul]\n  _ = 2 * n := by rfl\nStep-wise reasoning over transitive relations.calc\n  a = b := pab\n  b = c := pbc\n  ...\n  y = z := pyz\nproves a = z from the given step-wise proofs. = can be replaced with any\nrelation implementing the typeclass Trans. Instead of repeating the right-\nhand sides, subsequent left-hand sides can be replaced with _.calc\n  a = b := pab\n  _ = c := pbc\n  ...\n  _ = z := pyz\nIt is also possible to write the first relation as <lhs>\\n  _ = <rhs> := <proof>. This is useful for aligning relation symbols, especially on longer\nidentifiers:calc abc\n  _ = bce := pabce\n  _ = cef := pbcef\n  ...\n  _ = xyz := pwxyz\ncalc works as a term, as a tactic or as a conv tactic.See Theorem Proving in Lean 4 for more information.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Lean\u0009Tactics","header":"3.4.7. calc","id":"/Lean/#calc"}});