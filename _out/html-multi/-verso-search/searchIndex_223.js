window.docContents[223].resolve({"/Advanced-Mathematics/#product-topology":{"contents":"Given topological spaces α and β, the product α × β carries the\nproduct topology, where a basis of open sets is given by products of open\nsets.\n\n-- The product of topological spaces is a topological space\n#check (inferInstance : TopologicalSpace (ℝ × ℝ))\n\n-- Projections are continuous\nexample : Continuous (Prod.fst : ℝ × ℝ → ℝ) :=\n  continuous_fst\n\nexample : Continuous (Prod.snd : ℝ × ℝ → ℝ) :=\n  continuous_snd\n\n-- A function into a product is continuous iff both components are\n#check @Continuous.prodMk\n\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Advanced Mathematics\u0009Topology via Filters","header":"8.4.9. Product topology","id":"/Advanced-Mathematics/#product-topology"},"/Functional-Programming/#bind-operation":{"contents":"The key operation of a monad is bind (also written >>=). For Option, it works like this:\n\ndef myBind (x : Option α) (f : α → Option β) : Option β :=\n  match x with\n  | none => none\n  | some a => f a\n\n\nIt says: if x is none, propagate the failure; if x is some a, apply f to a. Now we can rewrite our chained division:\n\ndef chainDivide' (a b c : ℕ) : Option ℕ :=\n  safeDivide a b >>= fun r => safeDivide r c\n\n\nThis reads naturally: \"divide a by b, then take the result r and divide it by c.\"\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Functional Programming\u0009Monads and Computation Patterns","header":"6.4.2. The  Bind  operation","id":"/Functional-Programming/#bind-operation"},"/Tactics/#calc":{"contents":"Summary: As the word suggests, calc is about concrete calculations. This is not a tactic, but a lean mode. This means that you can enter this mode (with the word calc) and enter calculation steps and proofs that each individual calculation step is correct.\n\nExamples\n\nHere is a proof of the first binomial formula that only comes about by rewriting of calculating properties from the mathlib.\n\nexample (n : ℕ): (n+1)^2 = n^2 + 2*n + 1 := by\n  have h : n + n = 2 * n := by\n    nth_rewrite 1 [← one_mul n]\n    nth_rewrite 2 [← one_mul n]\n    rw [← add_mul]\n    rfl\n  calc (n+1)^2 = (n+1) * (n+1) := by rw [pow_two]\n  _ = (n + 1) * n + (n + 1) * 1 := by rw [mul_add]\n  _ = n * n + 1 * n + (n + 1) := by\n    rw [add_mul, mul_one (n + 1)]\n  _ = n^2 + n + (n + 1) := by rw [one_mul, ← pow_two]\n  _ = n^2 + (n + n + 1) := by\n    rw [add_assoc, ← add_assoc n n 1]\n  _ = n^2 + 2*n + 1 := by rw [← add_assoc, ← h]\n\n\nThe same can be achieved without the calc mode, like this:\n\nexample (n : ℕ): (n+1)^2 = n^2 + 2*n + 1 := by\n  have h : n + n = 2*n, by { nth_rewrite 0 ← one_mul n,\n  nth_rewrite 1 ← one_mul n, rw ← add_mul, },\n  rw [pow_two, mul_add, add_mul, mul_one (n+1), one_mul,\n  ← pow_two, add_assoc, ← add_assoc n n 1,\n  ← add_assoc, ← h],\n\n\nHowever, this is much less readable.\n\nRemarks\n\n* The exact notation is important in calc mode.* The calc mode not only works for equalities, but also for inequalities, subset-relations etc.* The example above can be solved easily using linarith or ring.* In order to generate a proof in calc mode, one can do it as follows (see the example below):* give the exact calculation steps without proof (using by sorry)* fill in the proofs which are left over.\n\n Here is how to start the proof of the binomial formula. First, leave out all proofs:example (n : ℕ) : n + n = 2*n := by\n  calc n + n = 1 * n + 1 * n := by sorry\n  _ = (1 + 1) * n := by sorry\n  _ = 2 * n := by sorry\nThen, fill in the detailsexample (n : ℕ) : n + n = 2*n := by\n  calc n + n = 1 * n + 1 * n := by rw [one_mul]\n  _ = (1 + 1) * n := by rw [add_mul]\n  _ = 2 * n := by rfl\nStep-wise reasoning over transitive relations.calc\n  a = b := pab\n  b = c := pbc\n  ...\n  y = z := pyz\nproves a = z from the given step-wise proofs. = can be replaced with any\nrelation implementing the typeclass Trans. Instead of repeating the right-\nhand sides, subsequent left-hand sides can be replaced with _.calc\n  a = b := pab\n  _ = c := pbc\n  ...\n  _ = z := pyz\nIt is also possible to write the first relation as <lhs>\\n  _ = <rhs> := <proof>. This is useful for aligning relation symbols, especially on longer\nidentifiers:calc abc\n  _ = bce := pabce\n  _ = cef := pbcef\n  ...\n  _ = xyz := pwxyz\ncalc works as a term, as a tactic or as a conv tactic.See Theorem Proving in Lean 4 for more information.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Tactics","header":"4.7. calc","id":"/Tactics/#calc"},"/Type-Theory/#axiom-quot":{"contents":"Lean has built-in support for quotient types. Given a type α and an equivalence relation r on α, you can form the quotient type Quot r. The axiom Quot.sound says that if r a b, then Quot.mk r a = Quot.mk r b.\n\nThis is how many mathematical constructions are formalized: the integers as a quotient of ℕ × ℕ, the rationals as a quotient of ℤ × ℤ, the reals as a quotient of Cauchy sequences, and so on.\n\n-- Quotient types are built into Lean\n#check @Quot.mk\n#check @Quot.sound\n#check @Quot.lift\n\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Type Theory\u0009Universes, Axioms, and Foundations\u0009The axioms of Lean","header":"7.3.4.3. The axiom of quotients","id":"/Type-Theory/#axiom-quot"},"/Type-Theory/#vectors-indexed":{"contents":"A classic example of dependent types is the type of vectors (lists of a fixed length). In Mathlib, one way to represent a vector of length n with entries in α is as a function Fin n → α, where Fin n is the type with exactly n elements {0, 1, ..., n-1}.\n\n-- A vector of 3 natural numbers, represented as a function from Fin 3\ndef myVec : Fin 3 → ℕ := ![10, 20, 30]\n\n#eval myVec 0   -- 10\n#eval myVec 1   -- 20\n#eval myVec 2   -- 30\n\n\nThe advantage of this representation is that indexing is always safe: you cannot access index 5 of a 3-element vector, because 5 is not a term of type Fin 3. The type system prevents out-of-bounds errors at compile time.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Type Theory\u0009Dependent Types in Depth","header":"7.2.5. Vectors indexed by length","id":"/Type-Theory/#vectors-indexed"}});