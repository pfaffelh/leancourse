window.docContents[145].resolve({"/Advanced-Mathematics/#state-monad":{"contents":"The StateM monad allows computations that read and modify state. A value of type StateM σ α is a computation that can access and modify state of type σ and produces a result of type α.\n\ndef counter : StateM ℕ ℕ := do\n  let n ← get\n  set (n + 1)\n  return n\n\ndef countThree : StateM ℕ (ℕ × ℕ × ℕ) := do\n  let a ← counter\n  let b ← counter\n  let c ← counter\n  return (a, b, c)\n\n#eval countThree.run 0    -- outputs ((0, 1, 2), 3)\n\n\nThe .run function executes the stateful computation with an initial state. The result ((0, 1, 2), 3) tells us the three counter values were 0, 1, 2, and the final state is 3.\n\nThe type StateM σ α is defined as σ → (α × σ), i.e. a function that takes an initial state and returns a result together with an updated state. The monad instance is:\n\n* pure (a : α) : StateM σ α := fun s => (a, s) -- returns a and leaves the state unchanged.* bind (x : StateM σ α) (f : α → StateM σ β) : StateM σ β := fun s => let (a, s') := x s; f a s' -- runs x with state s, obtaining result a and new state s', then runs f a with state s'.\n\nThe operations get : StateM σ σ := fun s => (s, s) and set (s' : σ) : StateM σ Unit := fun _ => ((), s') allow reading and writing the state.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Advanced Mathematics\u0009Monads and Probability Mass Functions\u0009Monads: the general pattern","header":"5.6.1.8. Stateful computation with  StateM","id":"/Advanced-Mathematics/#state-monad"},"/Type-Theory/#prop-vs-type":{"contents":"The universe Prop has a special property that distinguishes it from all Type n: proof irrelevance. This means that if P : Prop and h1 h2 : P, then h1 = h2. In other words, all proofs of the same proposition are considered equal.\n\nThis is a deliberate design choice. It means:\n\n* You cannot pattern-match on a proof to extract computational data.* Proofs can be erased at compile time without affecting program behavior.* Two mathematical proofs of the same theorem are interchangeable.\n\n-- Proof irrelevance: any two proofs of the same Prop are equal\nexample (P : Prop) (h1 h2 : P) : h1 = h2 :=\n  rfl\n\n-- This is why ∃ lives in Prop: you cannot extract the witness computationally\n-- If you need the witness, use Σ (which lives in Type)\n\n\nThe distinction between Prop and Type corresponds to the mathematical distinction between \"asserting that something exists\" (Prop) and \"constructing a specific example\" (Type).\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Type Theory\u0009Universes, Axioms, and Foundations","header":"4.3.2. Prop vs Type: proof irrelevance","id":"/Type-Theory/#prop-vs-type"}});