window.docContents[58].resolve({"/Advanced-Mathematics/#functor-typeclass":{"contents":"Before we discuss monads, we need the simpler concept of a functor. In Lean, Functor is a typeclass for type constructors f : Type → Type that support a map operation:\n\n* map : (α → β) → f α → f β -- applies a function to the value(s) inside the container, without changing the structure.\n\nThe infix notation for map is <$>, so g <$> x means Functor.map g x. Every monad is automatically a functor (since map can be defined in terms of bind and pure), but the functor interface is useful on its own when you only need to transform values without chaining computations.\n\nHere is what map does for each of our examples:\n\n* Option: map f x applies f to the contained value if x = some a, giving some (f a), and returns none if x = none.\n\n#eval (· + 10) <$> (some 5 : Option ℕ)  -- some 15\n#eval (· + 10) <$> (none : Option ℕ)    -- none\n\n\n* List: map f xs applies f to every element of the list, i.e. List.map f xs.\n\n#eval (· * 2) <$> [1, 2, 3]    -- [2, 4, 6]\n\n\n* IO: map f action produces a new IO action that runs action and then applies f to its result.* StateM σ: map f x runs the stateful computation x, obtains result a and new state s', and returns (f a, s'). The state is threaded through unchanged.\n\n#eval (Functor.map (· * 10) (get : StateM ℕ ℕ)).run 7\n-- outputs (70, 7)\n\n\n* Except ε: map f x applies f to the value if x = Except.ok a, giving Except.ok (f a), and returns Except.error e unchanged if x = Except.error e.\n\n#eval (· + 1) <$> (Except.ok 5 : Except String ℕ)\n-- Except.ok 6\n#eval (· + 1) <$> (Except.error \"fail\" : Except String ℕ)\n-- Except.error \"fail\"\n\n\n* Set: map f s is the image of s under f, i.e. {f a | a ∈ s}. This is the familiar image operation from mathematics.* Filter: Filter.map f F is the pushforward filter. A set s belongs to Filter.map f F if and only if f ⁻¹' s ∈ F. This generalizes the image operation to filters.* Finset: map f s applies an embedding f to each element of the finite set. For a plain function, one uses Finset.image f s instead.\n\nIn summary, map always means \"apply a function inside the container.\" The key property is that map preserves the structure: it cannot change none to some, shorten a list, or introduce new side effects. It only transforms the values.\n\n","context":"Interactive Theorem Proving using Lean, Winter 2026/27\u0009Advanced Mathematics\u0009Monads and Probability Mass Functions\u0009Monads: the general pattern","header":"5.6.1.4. The  Functor  typeclass","id":"/Advanced-Mathematics/#functor-typeclass"}});